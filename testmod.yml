- name: test my new module
  hosts: localhost
  tasks:
    - name: run the new module
      my_test2:
        name: 'hello'
        new: true
      register: testout
    - name: dump test output
      debug:
        msg: '{{ testout }}'

    - name: user data module test 
        userdata: name=Harshal age=34
